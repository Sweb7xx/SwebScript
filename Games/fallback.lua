-- Fallback script
print("SwebScript fallback loaded")
local notification = Instance.new("Message")
notification.Text = "SwebScript loaded in fallback mode"
notification.Parent = game.Workspace
wait(3)
notification:Destroy()
